# Copy this file to .env and fill in real values
# DO NOT commit the actual .env file

# Backend API URL
# Use your machine's LAN IP (e.g. 192.168.1.5) — NOT localhost —
# so Expo Go on a physical device can reach the backend.
API_BASE_URL=http://192.168.1.5:5000
